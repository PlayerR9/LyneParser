package StatusTree

/*
var oldPointer *TreeNode[uc.Enumer, *gr.LeafToken] // replace Type with the type of your pointer

func functionThatMightChangePointer() {
	currentPointer := getPointer() // replace this with the function or method that returns the pointer

	if oldPointer != nil && oldPointer != currentPointer {
		buf := make([]byte, 1024)
		runtime.Stack(buf, false)
		fmt.Printf("Pointer changed from %p to %p\nStack trace:\n%s\n", oldPointer, currentPointer, buf)
	}

	oldPointer = currentPointer
}
*/
